import React from 'react';

const Loading = () => {
  return (
    <div style={{ textAlign: 'center', marginTop: '50px' }}>
      <p>Loading...</p>
      {/* 여기에 로딩 중에 표시할 내용을 추가할 수 있습니다. */}
    </div>
  );
};

export default Loading;